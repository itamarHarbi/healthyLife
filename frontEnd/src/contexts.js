import { createContext } from "react";

export const CreateMenuSelectedProdArr = createContext([]);
export const IsSignedInContext = createContext(false);
export const CreateMenuDataContext = createContext(false);
export const UserIdContext = createContext(null);
export const ProfilePageContext = createContext(null);
